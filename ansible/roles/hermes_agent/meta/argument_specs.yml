---
argument_specs:
  main:
    short_description: Install and configure a locked-down Nous Hermes Agent.
    description:
      - Installs a checksum-pinned Hermes Agent revision under a root-owned path.
      - Installs a checksum-pinned ARM64 Codex CLI release.
      - Runs Hermes under a dedicated unprivileged system identity.
      - Manages a private or authenticated network dashboard and optional state backups.
      - Does not perform provider login or store operator credentials.
    options:
      hermes_agent_user:
        type: str
        default: hermes-agent
        description: Dedicated Hermes runtime user.
      hermes_agent_group:
        type: str
        default: hermes-agent
        description: Dedicated Hermes runtime group.
      hermes_agent_home:
        type: path
        default: /var/lib/hermes-agent
        description: Private persistent Hermes data directory.
      hermes_agent_install_dir:
        type: path
        default: /usr/local/lib/hermes-agent
        description: Root-owned Hermes source and runtime directory.
      hermes_agent_expected_repo_origin:
        type: str
        default: https://github.com/NousResearch/hermes-agent.git
        description: Exact Git remote origin required for an existing checkout.
      hermes_agent_binary_path:
        type: path
        default: /usr/local/bin/hermes
        description: Hermes CLI path created by the upstream installer.
      hermes_agent_repo_version:
        type: str
        default: 3c27eb6234bf91b8ceee9e9071591b31e9b148cb
        description: Full upstream Hermes Git commit to install.
      hermes_agent_version:
        type: str
        default: "0.20.0"
        description: Expected Hermes version reported by the CLI.
      hermes_agent_installer_url:
        type: str
        description: Checksum-pinned upstream Hermes installer URL.
      hermes_agent_installer_path:
        type: path
        default: /usr/local/libexec/hermes-agent-installer
        description: Root-owned local Hermes installer path.
      hermes_agent_installer_checksum:
        type: str
        description: SHA-256 checksum for the Hermes installer.
      hermes_agent_uv_python_install_dir:
        type: path
        default: /usr/local/share/uv/python
        description: System-readable directory for uv-managed Python runtimes.
      hermes_agent_uv_python_bin_dir:
        type: path
        default: /usr/local/share/uv/bin
        description: System-readable directory for uv-managed Python binaries.
      hermes_agent_supported_architectures:
        type: list
        elements: str
        default:
          - aarch64
          - arm64
        description: Target CPU architectures allowed to install the ARM64 Codex CLI.
      hermes_agent_packages:
        type: list
        elements: str
        description: Debian packages installed before Hermes.
      hermes_agent_model:
        type: str
        default: gpt-5.5
        description: Initial provider model identifier.
      hermes_agent_provider:
        type: str
        default: openai-codex
        choices:
          - openai-codex
        description: Hermes provider configured by this locked-down role.
      hermes_agent_fallback_providers:
        type: list
        elements: dict
        default: []
        description: Ordered provider/model pairs used only after primary-provider failures.
      hermes_agent_reasoning_effort:
        type: str
        default: medium
        choices:
          - none
          - minimal
          - low
          - medium
          - high
          - xhigh
          - max
          - ultra
        description: Default reasoning effort for Hermes model calls.
      hermes_agent_reasoning_overrides:
        type: dict
        default: {}
        description: Per-model reasoning-effort overrides.
      hermes_agent_model_aliases:
        type: dict
        default: {}
        description: Named session model selectors with provider and model values.
      hermes_agent_manage_inference_provenance:
        type: bool
        default: false
        description: Install and enable the managed response-provenance audit plugin.
      hermes_agent_inference_provenance_plugin_name:
        type: str
        default: inference-provenance
        description: Managed Hermes plugin identifier for response provenance.
      hermes_agent_inference_provenance_label:
        type: str
        default: Codex
        description: Deployment-specific provider label appended to responses.
      hermes_agent_inference_provenance_default_models:
        type: list
        elements: str
        default: []
        description: Models recorded as the configured default route by the provenance plugin.
      hermes_agent_inference_provenance_fallback_models:
        type: list
        elements: str
        default: []
        description: Models recorded as fallback or operator-selected routes by the provenance plugin.
      hermes_agent_manage_home_assistant_mcp:
        type: bool
        default: false
        description: Enable the Home Assistant Assist MCP capability gateway.
      hermes_agent_home_assistant_mcp_name:
        type: str
        default: home-assistant-assist
        description: Hermes MCP server name for the Home Assistant Assist API.
      hermes_agent_home_assistant_mcp_url:
        type: str
        default: ""
        description: Full Home Assistant Assist MCP endpoint URL.
      hermes_agent_home_assistant_mcp_token_env_file:
        type: path
        description: Private environment file containing the Home Assistant token.
      hermes_agent_home_assistant_mcp_token_env_var:
        type: str
        default: HASS_MCP_TOKEN
        description: Environment variable name used for the Home Assistant token.
      hermes_agent_home_assistant_mcp_timeout:
        type: int
        default: 30
        description: Maximum seconds for an individual Home Assistant MCP request.
      hermes_agent_home_assistant_mcp_connect_timeout:
        type: int
        default: 15
        description: Maximum seconds for initial Home Assistant MCP connection.
      hermes_agent_enabled_toolsets:
        type: list
        elements: str
        description: Explicit Hermes CLI toolset allowlist.
      hermes_agent_disabled_toolsets:
        type: list
        elements: str
        description: Hermes toolsets explicitly disabled by policy.
      hermes_agent_dashboard_host:
        type: str
        default: 127.0.0.1
        choices:
          - 127.0.0.1
          - 0.0.0.0
        description: Loopback or wildcard IPv4 address used by the dashboard.
      hermes_agent_dashboard_port:
        type: int
        default: 9119
        description: Dashboard TCP port.
      hermes_agent_dashboard_service_name:
        type: str
        default: hermes-agent-dashboard.service
        description: Systemd unit name for the dashboard.
      hermes_agent_dashboard_service_managed_state_file:
        type: path
        default: /etc/systemd/system/.hermes-agent-dashboard-managed.yml
        description: Root-only provenance record for the managed dashboard unit.
      hermes_agent_manage_dashboard:
        type: bool
        default: true
        description: Whether to build and run the dashboard.
      hermes_agent_manage_dashboard_chat:
        type: bool
        default: false
        description: Build a separate prebuilt terminal UI bundle for dashboard chat.
      hermes_agent_dashboard_tui_runtime_dir:
        type: path
        default: /usr/local/lib/hermes-agent-runtime/tui
        description: Root-owned prebuilt terminal UI bundle directory.
      hermes_agent_dashboard_tui_runtime_revision_file:
        type: path
        description: Root-owned marker recording the Hermes revision used to build the terminal UI bundle.
      hermes_agent_dashboard_tui_runtime_manifest_file:
        type: path
        description: Root-owned content fingerprint for the managed terminal UI runtime.
      hermes_agent_dashboard_manage_basic_auth:
        type: bool
        default: false
        description: Generate and manage Hermes native dashboard password authentication.
      hermes_agent_dashboard_basic_auth_username:
        type: str
        default: ""
        description: Username for Hermes native dashboard password authentication.
      hermes_agent_dashboard_basic_auth_state_file:
        type: path
        description: Private file holding the dashboard password hash and session secret.
      hermes_agent_dashboard_basic_auth_bootstrap_password_file:
        type: path
        description: Root-only initial dashboard password retrieval file.
      hermes_agent_dashboard_basic_auth_session_ttl_seconds:
        type: int
        default: 43200
        description: Authenticated dashboard session lifetime in seconds.
      hermes_agent_dashboard_rotate_basic_auth:
        type: bool
        default: false
        description: Generate a replacement dashboard password and session secret once.
      hermes_agent_codex_version:
        type: str
        default: "0.145.0"
        description: Codex CLI release version.
      hermes_agent_download_cache_dir:
        type: path
        default: /var/cache/hermes-agent
        description: Root-owned cache for verified download artifacts.
      hermes_agent_codex_archive_url:
        type: str
        description: Checksum-pinned ARM64 Codex archive URL.
      hermes_agent_codex_archive_checksum:
        type: str
        description: SHA-256 checksum for the Codex archive.
      hermes_agent_codex_archive_binary_name:
        type: str
        default: codex-aarch64-unknown-linux-musl
        description: Codex executable name inside the downloaded archive.
      hermes_agent_codex_install_dir:
        type: path
        description: Versioned root-owned Codex installation directory.
      hermes_agent_codex_binary_path:
        type: path
        default: /usr/local/bin/codex
        description: Symlink path for the active Codex CLI.
      hermes_agent_codex_home:
        type: path
        description: Private Codex state directory for the Hermes runtime user.
      hermes_agent_manage_backup:
        type: bool
        default: false
        description: Whether to schedule full Hermes state backups.
      hermes_agent_backup_group:
        type: str
        default: ""
        description: Existing group that grants the runtime user backup-path access.
      hermes_agent_backup_dir:
        type: path
        default: /var/backups/hermes-agent
        description: Private directory for full Hermes backup archives.
      hermes_agent_manage_local_models:
        type: bool
        default: false
        description: Install local llama.cpp benchmark services and model files.
      hermes_agent_local_model_packages:
        type: list
        elements: str
        description: Debian packages required by the local-model benchmark helper.
      hermes_agent_local_model_runtime_version:
        type: str
        default: b9637
        description: llama.cpp release identifier.
      hermes_agent_local_model_runtime_archive_url:
        type: str
        description: Checksum-pinned ARM64 llama.cpp archive URL.
      hermes_agent_local_model_runtime_archive_checksum:
        type: str
        description: SHA-256 checksum for the llama.cpp archive.
      hermes_agent_local_model_runtime_dir:
        type: path
        description: Versioned root-owned llama.cpp installation directory.
      hermes_agent_local_model_llama_cli_path:
        type: path
        default: /usr/local/bin/llama-cli
        description: Symlink path for the active llama-cli binary.
      hermes_agent_local_model_llama_server_path:
        type: path
        default: /usr/local/bin/llama-server
        description: Symlink path for the active llama-server binary.
      hermes_agent_local_models_dir:
        type: path
        description: Directory for checksum-pinned local model files.
      hermes_agent_local_models_results_dir:
        type: path
        description: Directory for local-model benchmark results.
      hermes_agent_local_models_min_free_bytes:
        type: int
        default: 10737418240
        description: Free-space reserve required after model downloads.
      hermes_agent_local_models_host:
        type: str
        default: 127.0.0.1
        choices:
          - 127.0.0.1
        description: Loopback address used by local llama.cpp services.
      hermes_agent_local_models_context_size:
        type: int
        default: 65536
        description: Server context size used to meet Hermes local-model policy.
      hermes_agent_local_models_threads:
        type: int
        default: 3
        description: Inference and batch thread count for each local model service.
      hermes_agent_local_models_benchmark_max_tokens:
        type: int
        default: 128
        description: Completion-token budget for the synthetic local-model probe.
      hermes_agent_local_models_benchmark_request_timeout:
        type: int
        default: 120
        description: Maximum seconds allowed for the synthetic local-model request.
      hermes_agent_local_models_cache_type_k:
        type: str
        default: q4_0
        description: Key-cache quantization used to constrain local-model memory.
      hermes_agent_local_models_cache_type_v:
        type: str
        default: q4_0
        description: Value-cache quantization used to constrain local-model memory.
      hermes_agent_local_models_address_space_limit:
        type: str
        default: 4G
        description: Process address-space hard limit for each local model service.
      hermes_agent_local_models_memory_high:
        type: str
        default: 2500M
        description: systemd MemoryHigh limit for each local model service.
      hermes_agent_local_models_memory_max:
        type: str
        default: 3200M
        description: systemd MemoryMax limit for each local model service.
      hermes_agent_local_models_memory_swap_max:
        type: str
        default: 512M
        description: systemd MemorySwapMax limit for each local model service.
      hermes_agent_local_model_benchmark_helper_path:
        type: path
        default: /usr/local/libexec/hermes-agent/benchmark-local-model
        description: Installed helper used to benchmark one local model service.
      hermes_agent_local_models_managed_state_file:
        type: path
        default: /etc/systemd/system/hermes-local-models.managed.yml
        description: Canonical root-owned checksum manifest of managed local-model units.
      hermes_agent_local_models:
        type: list
        elements: dict
        description: >-
          Model records with id, display_name, repository, revision, filename,
          sha256, size_bytes, and port fields.
      hermes_agent_backup_service_name:
        type: str
        default: hermes-agent-backup.service
        description: Systemd unit name for the backup service.
      hermes_agent_backup_service_managed_state_file:
        type: path
        default: /etc/systemd/system/.hermes-agent-backup-service-managed.yml
        description: Root-only provenance record for the managed backup service.
      hermes_agent_backup_timer_name:
        type: str
        default: hermes-agent-backup.timer
        description: Systemd unit name for the backup timer.
      hermes_agent_backup_timer_managed_state_file:
        type: path
        default: /etc/systemd/system/.hermes-agent-backup-timer-managed.yml
        description: Root-only provenance record for the managed backup timer.
      hermes_agent_backup_retention_days:
        type: int
        default: 30
        description: Number of days to retain backup archives.
      hermes_agent_backup_on_calendar:
        type: str
        default: "*-*-* 03:20:00"
        description: systemd.time calendar expression for scheduled backups.
      hermes_agent_backup_timezone:
        type: str
        default: UTC
        description: Time zone appended to the backup schedule.
      hermes_agent_backup_randomized_delay:
        type: str
        default: 30m
        description: Maximum randomized backup start delay.
