---
- name: Preflight PiServ external storage
  hosts: piserv
  gather_facts: true
  become: true
  tasks:
    - name: Validate external storage before host mutation
      ansible.builtin.import_tasks: ../tasks/external-storage-preflight.yml
