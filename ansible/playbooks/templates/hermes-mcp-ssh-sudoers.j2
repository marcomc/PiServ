# Managed by Ansible. Permit only the no-argument Hermes MCP entry point.
Defaults:{{ piserv_hermes_mcp_ssh_user }} !use_pty
{{ piserv_hermes_mcp_ssh_user }} ALL=(root) NOPASSWD: {{ piserv_hermes_mcp_ssh_wrapper_path }} ""
