---
# External storage is discovered at runtime from the managed filesystem label.
# The playbook requires exactly one candidate and derives its parent disk and
# udev identity without formatting or adopting an unrelated disk.
